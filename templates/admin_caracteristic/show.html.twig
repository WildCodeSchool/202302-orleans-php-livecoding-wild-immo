{% extends 'base.html.twig' %}

{% block title %}Caracteristic{% endblock %}

{% block body %}
    <h1>Caracteristic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caracteristic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ caracteristic.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ caracteristic.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_caracteristic_index') }}">back to list</a>

    <a href="{{ path('app_admin_caracteristic_edit', {'id': caracteristic.id}) }}">edit</a>

    {{ include('admin_caracteristic/_delete_form.html.twig') }}
{% endblock %}
